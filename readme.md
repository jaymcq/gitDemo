Hello World!
.md is markdown and you can use # to create sections for notes
'''    ''' Inbetween three ticks you can place a block of code

# Git Notes

## Enable Powershell
To enable Powershell in VS Tools->Command Line->Terminal
Make sure Powershell shows correct location

## Git Commands

'''
git status = check if current is a git repository and check status

git init = make current a git repository can remove ".git" file to disconnect

git add <file name>

git commit -m '<Add message here>'

git log = list all commits made

git add -A = adds all files not on the gitignore

git tag -a '<version>' -m '<release>' = adds a tag

git remote add origin <link to github> = remote add called 'orgin' to url

git push origin master = push local to github

git push origin <branch name> = push local branch to github

git push origin --tags = push all tags to github

git checkout -b <branch name> = create a new branch and switch to it

git checkout <branch name> = switch to a branch

'''